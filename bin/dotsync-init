#!/usr/bin/env bash
# dotsync init — first-machine setup wizard.
# Engine: syncthing (only engine in v0.4+).
set -uo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec "$SCRIPT_DIR/dotsync-init-syncthing" "$@"
