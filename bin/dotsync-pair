#!/usr/bin/env bash
# dotsync pair — emit a pairing code so a second machine can join.
# Engine: syncthing.
set -uo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec "$SCRIPT_DIR/dotsync-pair-syncthing" "$@"
