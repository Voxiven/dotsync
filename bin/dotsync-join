#!/usr/bin/env bash
# dotsync join — set up a second machine using a pairing code from the first.
# Engine: syncthing.
set -uo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec "$SCRIPT_DIR/dotsync-join-syncthing" "$@"
